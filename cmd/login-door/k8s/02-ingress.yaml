apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: login-door-npool-top-ingress
  namespace: kube-system
  annotations:
    k8s.kuboard.cn/displayName: login-door
    k8s.kuboard.cn/workload: login-door
    kubernetes.io/ingress.class: "traefik-internet"
spec:
  rules:
  - host: api.npool.top
    http:
      paths:
      - path: /login-door
        backend:
          serviceName: login-door
          servicePort: http

---

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: login-door-xpool-top-ingress
  namespace: kube-system
  annotations:
    k8s.kuboard.cn/displayName: login-door
    k8s.kuboard.cn/workload: login-door
    kubernetes.io/ingress.class: "traefik-internet"
spec:
  rules:
  - host: api.xpool.top
    http:
      paths:
      - path: /login-door
        backend:
          serviceName: login-door
          servicePort: http
